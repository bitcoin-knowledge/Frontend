

const initialState = {
  folderName: '',
  userTwtrData: false,
};

const ChatbotReducer = function (state = initialState, action: any) {
  switch (action.type) {
    default:
      return state;
  }
};

export default ChatbotReducer;