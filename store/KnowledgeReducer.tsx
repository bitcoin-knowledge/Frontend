

const initialState = {
    folderName: '',
    userTwtrData: false,
  };
  
  const KnowledgeReducer = function (state = initialState, action: any) {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default KnowledgeReducer;